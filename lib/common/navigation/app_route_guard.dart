class AppRouteGuard {
  /// Auth guard logic for redirections
  // checkAuth(BuildContext context, GoRouterState state) {
  //   User? user = _hive.get(_hive.userBox, 'user');
  //   if (user != null && user.accessToken != null) {
  //     return null;
  //   } else {
  //     return '/signin';
  //   }
  // }
}
